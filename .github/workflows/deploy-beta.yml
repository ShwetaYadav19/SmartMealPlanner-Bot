name: Deploy Beta

on:
  push:
    branches: [beta]

permissions:
  id-token: write
  contents: read

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - run: npm ci

      - name: Run tests
        run: npm test

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN_BETA }}
          aws-region: ap-south-1

      - name: Build & Deploy
        env:
          STAGE: beta
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID_BETA }}
          TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN_BETA }}
          TWILIO_SENDER_NUMBER: ${{ secrets.TWILIO_SENDER_NUMBER_BETA }}
        run: |
          npm run build
          node scripts/deploy.js
